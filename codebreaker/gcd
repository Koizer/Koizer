#include<bits/stdc++.h>
using namespace std;


int gcds(int a, int b){
    if(b>a) swap(a,b);
    if(b==0) return a;

    return gcds(b,a%b);
}


int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);

	int a,b;
	cin>>a>>b;
	cout<<gcds(a,b);




return 0;
}
