#include<bits/stdc++.h>
using namespace std;

#define fastio ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0)

int main(){

	long long n,a,b,mx=-1;
	
	cin>>n;
	
	while(n--){
		cin>>a>>b;
		mx=max(a*b,mx);
	}
	
	cout<<mx;

}
