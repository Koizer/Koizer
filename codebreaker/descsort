#include<bits/stdc++.h>
using namespace std;


int main(){
	long n,a;
	vector<long> arr;
	
	cin>>n;
	for(int i=0;i<n;i++){
		cin>>a;
		arr.push_back(a);
	}
	
	sort(arr.begin(), arr.end(), greater<int>());
	
	for(long k:arr)cout<<k<<' ';

}
